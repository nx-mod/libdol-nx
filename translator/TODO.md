# TODO - translator

- [ ] Emit `<wiinx/cpu/...>` includes rather than bare names, which is what ends
      the split between public and internal headers
- [ ] GameCube executables: the same instruction set, a different boot path and
      an apploader
- [ ] Relocatable modules for GameCube games, as REL is handled for the Wii
- [ ] Say which functions were replaced by natives in the build log, so a slow
      frame can be read against what was not
