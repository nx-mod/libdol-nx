# TODO - docs

- [ ] Disc images: the layout both consoles use, once `format/disc` exists
- [ ] Memory cards and ARAM, which libgc-nx will need described
- [ ] A page for the launcher: what it shows and where it gets it
- [ ] One compatibility list, generated, rendered into both front repos rather
      than written in five READMEs that will disagree within a week
