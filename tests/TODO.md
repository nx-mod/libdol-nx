# TODO - tests

- [ ] A disc reader check: a synthetic image built here, and a real dump on a
      machine that has one
- [ ] A WAD round-trip against a real WAD, likewise
- [ ] A golden FIFO check, so the graphics decoder can be changed without a
      Switch to try it on
- [ ] Run the checks in CI on every push, which is the only automated
      correctness this library has
