# TODO - app

- [x] One launcher, in `src/launcher`: it lists what is installed under
      `sdmc:/wii-nx/<games|wads|titles>/` and hands over with envSetNextLoad.
      Still to do: themed per console, and banners rather than names
- [ ] A title list from the NAND, so installed channels appear beside games
- [ ] Settings a player can change while a game runs, with the ones that need a
      restart marked
