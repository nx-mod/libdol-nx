# TODO - core

- [ ] Typed guest arrays, so a native walking a table does not compute offsets
      by hand
- [ ] A const view over a guest structure, for natives that only read
- [ ] Catch a native registered against a build id no table knows, at startup
      rather than at binding time
