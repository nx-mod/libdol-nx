# TODO - format

- [ ] `disc`: the whole module ([detail](disc/TODO.md))
- [ ] `nand`: installing and removing titles, saves, ASH ([detail](nand/TODO.md))
- [ ] `archive`: U8 ([detail](archive/TODO.md))
- [ ] `media`: THP's audio track, and the container around its frames
      ([detail](media/TODO.md))
- [ ] A `texture` module: the GX texture formats as plain conversions, shared by
      the renderer, the tools and anything that wants a banner as a picture
