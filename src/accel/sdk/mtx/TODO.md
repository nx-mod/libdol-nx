# TODO - accel/sdk/mtx

- [ ] The rest of `rvl.mtx`: 8 of its 42 functions are written
- [ ] The vector and quaternion sets, which the same games call as often
- [ ] The GameCube's own builds of the same library, which differ only in date
