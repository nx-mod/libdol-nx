# TODO - accel/sdk/msl

- [x] `strcpy`, `strncpy`, `strcat`, `strncmp`, `memcmp`, `strchr`, `strrchr`
- [ ] `strstr`, `strtok`, `strspn` - less hot, still worth having
- [ ] `qsort`, which a few games call per frame
- [ ] The float formatting in `sprintf`, which is slow everywhere it is used
