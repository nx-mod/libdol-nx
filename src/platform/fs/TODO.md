# TODO - platform/fs

- [ ] Read from a disc image, so nothing has to be extracted
- [ ] Keep the disc's own timing when reading from a fast device: a game that
      hides loading behind an animation should still see the animation
- [ ] Memory cards, for a GameCube game (in libgc-nx)
