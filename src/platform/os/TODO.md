# TODO - platform/os

- [ ] Alarms from a timer rather than a polled queue
- [ ] Report, once, when a game installs an interrupt handler nothing will call
- [ ] Thread names in the log, so a stall says which thread stalled
