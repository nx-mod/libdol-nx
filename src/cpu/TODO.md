# TODO - cpu

- [ ] Move these headers under `include/wiinx/cpu/` and have the translator emit
      `<wiinx/cpu/...>`. One retranslate, and the two naming worlds become one
- [ ] `dolnx::` for what both consoles share
- [ ] Find a game's OS globals without being told: the scanner already finds
      most, and a game should have to install only what it could not
- [ ] Drop `mkw_` from the remaining file names
