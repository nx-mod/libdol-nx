# TODO - tools/src

- [ ] `wiinx-nand`: install a WAD into a NAND, list what is installed, and take
      a title back out, once `format/nand` can write
- [ ] Verify a dump against its own hashes, which needs the padding generator
- [ ] One front end (`wiinx <command>`) over these and the scripts
